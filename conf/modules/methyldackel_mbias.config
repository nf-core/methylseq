process {
    withName: METHYLDACKEL_MBIAS {
        ext.args = [
            params.all_contexts ? ' --CHG --CHH' : '',
            params.ignore_flags ? " --ignoreFlags" : ''
        ].join(" ").trim()
    }
}
