- name: bismark_hisat - Single End with index
  command: nextflow run main.nf -profile test,docker --aligner bismark_hisat --save_reference --bismark_index results/bismark_hisat/reference_genome/BismarkIndex/ --local_alignment --outdir ./results
  tags:
    - bismark_hisat
    - single-end
    - index
  # This ain't gonna work because they run in parallel
  exit_code: 1

- name: bismark_hisat - Single End RRBS with index
  command: nextflow run main.nf -profile test,docker --aligner bismark_hisat --skip_trimming --rrbs --bismark_index results/bismark_hisat/reference_genome/BismarkIndex/ --local_alignment --outdir ./results
  tags:
    - bismark_hisat
    - single-end
    - rrbs
    - index
  # This ain't gonna work because they run in parallel
  exit_code: 1
